# Server Configuration (Railway Dynamic Port)
server.port=${PORT:8080}

# Database Configuration (Railway)
spring.datasource.url=jdbc:mysql://nozomi.proxy.rlwy.net:41349/railway?createDatabaseIfNotExist=true&useUnicode=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=ZNZNGkayjGxrHilTlAAVWyEkepfhPYcw
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Connection Pooling (Better Performance)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2

# JPA & Hibernate Configuration (Optimized for Production)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.show_sql=false  # Disable in production for security
spring.jpa.properties.hibernate.format_sql=false  # Disable for performance

# SpringDoc API Docs Configuration
springdoc.packagesToScan=com.onlinebankingsystem.controller
springdoc.pathsToMatch=/api/**

# CORS Configuration (Allow only your frontend domain)
spring.web.cors.allowed-origins=https://your-frontend-domain.com
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE
spring.web.cors.allowed-headers=*
